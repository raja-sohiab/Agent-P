# Global Guardrails and Freeze State

- Treat every input lead as ICP-qualified; qualification remains upstream.
- Process each lead independently using the current evidence hierarchy.
- Never fabricate company facts, customer intent, capabilities, outcomes, or website observations.
- Never use fake personalization, “I noticed,” or “I came across.”
- No guarantees, hype, unnecessary links, aggressive CTAs, or em dash character `—`.
- Never use generic fallback templates, hard-coded mappings, external generic scripts, or a fallback writer.
- Preserve the CSV contract and release only after complete QA.

## Frozen state
The Email 1–3 knowledge base is currently **FROZEN**. Do not change writing rules, evidence rules, execution rules, or QA requirements without explicit future instruction. The authoritative production workflow is defined by `AGENT.md`, the named owner files, and `TESTING/PRODUCTION-TEST.md`.
